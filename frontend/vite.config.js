import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  css: {
    postcss: null // Deshabilitar PostCSS para evitar conflictos con Tailwind
  }
})